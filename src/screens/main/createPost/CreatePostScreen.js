import React from "react";
import { Button, Text, View } from "react-native";

const CreatePostScreen = ({ navigation }) => {
  return (
    <View>
      <Text>this is CreatePost screen</Text>
    </View>
  );
};

export default CreatePostScreen;
